import { CarpetaPropietario } from './carpeta-propietario';

describe('CarpetaPropietario', () => {
  it('should create an instance', () => {
    expect(new CarpetaPropietario()).toBeTruthy();
  });
});
